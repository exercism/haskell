name: lens-person

dependencies:
  - base
  - time

library:
  exposed-modules: Person
  source-dirs: src
  dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.
    - lens

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - lens-person
      - HUnit
