name: bob
version: 0.9.0.1 # 2016-11-29

dependencies:
  - base

library:
  exposed-modules: Bob
  source-dirs: src
  dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - bob
      - hspec
