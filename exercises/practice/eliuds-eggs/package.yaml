name: eliuds-eggs
version: 0.0.0.1

dependencies:
  - base

library:
  exposed-modules: EliudsEggs
  source-dirs: src
  ghc-options: -Wall
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - eliuds-eggs
      - hspec
