name: book-store

dependencies:
  - base

library:
  exposed-modules: BookStore
  source-dirs: src

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - book-store
      - hspec
