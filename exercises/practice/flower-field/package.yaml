name: flower-field
version: 1.1.0.5

dependencies:
  - base

library:
  exposed-modules: FlowerField
  source-dirs: src
  ghc-options: -Wall
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - flower-field
      - hspec
