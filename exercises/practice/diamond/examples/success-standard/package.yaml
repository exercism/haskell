name: diamond

dependencies:
  - base

library:
  exposed-modules: Diamond
  source-dirs: src

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - diamond
      - hspec
      - QuickCheck
      - text
      - string-conversions
