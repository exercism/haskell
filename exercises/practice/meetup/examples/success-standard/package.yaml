name: meetup

dependencies:
  - base
  - time

library:
  exposed-modules: Meetup
  source-dirs: src

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - meetup
      - hspec
