name: grep
version: 1.0.0.0

dependencies:
  - base
  
library:
  exposed-modules: Grep
  source-dirs: src
  ghc-options: -Wall

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - grep
      - hspec
      - directory
