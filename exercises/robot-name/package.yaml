name: robot-name

dependencies:
  - base

library:
  exposed-modules: Robot
  source-dirs: src
  dependencies:
    - random
  # - foo       # Here you must list additional packages
  # - bar       # you want to use in you solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - robot-name
      - HUnit
