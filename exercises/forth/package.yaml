name: forth
version: 1.5.0.7

dependencies:
  - base
  - text

library:
  exposed-modules: Forth
  source-dirs: src
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - forth
      - hspec
