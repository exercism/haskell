name: bank-account

dependencies:
  - base

library:
  exposed-modules: BankAccount
  source-dirs: src
  dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.
    - stm

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - bank-account
      - hspec
