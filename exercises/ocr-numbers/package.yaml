name: ocr-numbers
version: 0.9.0.1 # 2016-08-09

dependencies:
  - base

library:
  exposed-modules: OCR
  source-dirs: src
  dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - ocr-numbers
      - hspec
