name: protein-translation

dependencies:
  - base

library:
  exposed-modules: ProteinTranslation
  source-dirs: src

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - protein-translation
      - hspec
