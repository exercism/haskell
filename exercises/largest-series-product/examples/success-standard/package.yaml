name: largest-series-product

dependencies:
  - base

library:
  exposed-modules: Series
  source-dirs: src

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - largest-series-product
      - hspec
