name: dominoes
version: 1.0.1.4

dependencies:
  - base

library:
  exposed-modules: Dominoes
  source-dirs: src
  dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - dominoes
      - hspec
