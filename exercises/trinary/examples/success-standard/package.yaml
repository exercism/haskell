name: trinary

dependencies:
  - base

library:
  exposed-modules: Trinary
  source-dirs: src

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - trinary
      - hspec
      - QuickCheck
