name: acronym
version: 1.6.0.8

dependencies:
  - base

library:
  exposed-modules: Acronym
  source-dirs: src
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - acronym
      - hspec
