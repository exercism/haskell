name: acronym
version: 1.5.0.7

dependencies:
  - base

library:
  exposed-modules: Acronym
  source-dirs: src
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - acronym
      - hspec
