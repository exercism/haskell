name: bracket-push
version: 1.5.0.7

dependencies:
  - base

library:
  exposed-modules: Brackets
  source-dirs: src
  ghc-options: -Wall
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - bracket-push
      - hspec
