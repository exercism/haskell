name: parallel-letter-frequency

dependencies:
  - base
  - containers
  - text

library:
  exposed-modules: Frequency
  source-dirs: src
  dependencies:
    - parallel

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - parallel-letter-frequency
      - hspec
