name: isbn-verifier
version: 2.4.0.5

dependencies:
  - base

library:
  exposed-modules: IsbnVerifier
  source-dirs: src
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - isbn-verifier
      - hspec
