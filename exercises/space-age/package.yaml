name: space-age
version: 1.0.0.3

dependencies:
  - base

library:
  exposed-modules: SpaceAge
  source-dirs: src
  dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - space-age
      - hspec
