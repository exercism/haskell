name: linked-list

dependencies:
  - base

library:
  exposed-modules: Deque
  source-dirs: src
  dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.
    - stm

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - linked-list
      - HUnit
