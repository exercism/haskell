name: minesweeper

dependencies:
  - base

library:
  exposed-modules: Minesweeper
  source-dirs: src
  dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.
    - array

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - minesweeper
      - hspec
