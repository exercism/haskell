name: beer-song
version: 0.1.0.2

dependencies:
  - base

library:
  exposed-modules: Beer
  source-dirs: src
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - beer-song
      - hspec
