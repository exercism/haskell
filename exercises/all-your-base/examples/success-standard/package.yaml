name: all-your-base

dependencies:
  - base

library:
  exposed-modules: Base
  source-dirs: src

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - all-your-base
      - hspec
