name: saddle-points
version: 1.1.0.4

dependencies:
  - array
  - base

library:
  exposed-modules: Matrix
  source-dirs: src
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - saddle-points
      - hspec
