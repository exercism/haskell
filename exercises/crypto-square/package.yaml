name: crypto-square

dependencies:
  - base

library:
  exposed-modules: CryptoSquare
  source-dirs: src
  dependencies:
    - split
  # - foo       # Here you must list additional packages
  # - bar       # you want to use in you solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - crypto-square
      - HUnit
