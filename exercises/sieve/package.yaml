name: sieve
version: 1.1.0.4

dependencies:
  - base

library:
  exposed-modules: Sieve
  source-dirs: src
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - sieve
      - hspec
