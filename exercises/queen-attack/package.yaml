name: queen-attack
version: 2.1.0.5

dependencies:
  - base

library:
  exposed-modules: Queens
  source-dirs: src
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - queen-attack
      - hspec
