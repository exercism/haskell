name: phone-number

dependencies:
  - base

library:
  exposed-modules: Phone
  source-dirs: src
  dependencies:
  # - foo       # Here you must list additional packages
  # - bar       # you want to use in you solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - phone-number
      - HUnit
